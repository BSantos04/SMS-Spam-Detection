# Utilizar volumes no Docker:

## Ocupam espaço e guardam ficheiros de forma dinâmica
selecionar a flag "-v" ao correr o script no container

### Exemplo
docker run -it -v $(pwd)/models:/app/models sms "Hello World!" --verbose

# Usar joblib ou pickle

# Usar a opção sklearn make_pipeline
